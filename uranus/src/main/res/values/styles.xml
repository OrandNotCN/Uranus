<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@android:color/white</item>
        <item name="android:textColor">#000000</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppBaseTheme" parent="android:Theme.Holo.Light">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:textSize">15sp</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Theme.ToolBarStyle.Blur.TitleTextAppearance" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Image.Edit.Fill" parent="android:style/Widget.ImageWell">
        <item name="android:background">@color/black_alpha_224</item>
        <item name="android:layout_width">@dimen/activity_edit_item_height</item>
        <item name="android:layout_height">@dimen/activity_edit_item_height</item>
        <item name="android:padding">@dimen/dimen_8dp</item>
        <item name="android:layout_gravity">center_vertical</item>
    </style>

    <style name="Theme.Dialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@drawable/ic_dialog_material_background_light</item>
        <item name="windowMinWidthMajor">100%</item>
        <item name="windowMinWidthMinor">100%</item>
    </style>

</resources>
